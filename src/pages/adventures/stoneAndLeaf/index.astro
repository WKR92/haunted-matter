---
import Layout from '../../../layouts/Layout.astro';
import * as rules from '../../../content/obsidian/The boundary between stone and leaf.mdx';

const { frontmatter, Content } = rules;
---

<Layout title={frontmatter.title}>
  <h1>{frontmatter.title}</h1>
  <Content />
</Layout>